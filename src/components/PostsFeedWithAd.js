import React from "react"
import styled from "@emotion/styled"

const SectionWrapper = styled.section`
  padding-top: 80px;
`

const PostsFeedWithAd = () => {
  return <SectionWrapper>POSTS FEED AD</SectionWrapper>
}

export default PostsFeedWithAd
